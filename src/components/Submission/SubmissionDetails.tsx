import React from "react";

export interface ISubmission {
  submissionId: string;
}
const SubmissionDetails = (props: ISubmission) => {
  //Retrive submission
  //Show feedback
  return <div>SubmissionDetails</div>;
};

export default SubmissionDetails;
